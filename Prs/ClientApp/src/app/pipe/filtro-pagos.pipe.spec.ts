import { FiltroPagosPipe } from './filtro-pagos.pipe';

describe('FiltroPagosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPagosPipe();
    expect(pipe).toBeTruthy();
  });
});
